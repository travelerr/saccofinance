import {premiumDate,type OpportunityUpdate} from '@/lib/premium-content';
export default function OpportunityUpdates({updates}:{updates:OpportunityUpdate[]}){
 return updates.length?<ol className="opportunity-update-list">{updates.map(u=><li key={u.id}><p className="opportunity-meta"><time dateTime={u.eventDate||u.publishedAt}>{premiumDate(u.eventDate||u.publishedAt)}</time> · {u.tradeStatusBefore?`${u.tradeStatusBefore} → ${u.tradeStatusAfter}`:`Trade Status: ${u.tradeStatusAfter}`}</p>{u.eventDate&&u.eventDate!==u.publishedAt&&<p className="opportunity-meta">Historical event · Added to Premium archive {premiumDate(u.publishedAt)}</p>}{u.technicalStage&&<p className="opportunity-meta">Technical Stage: {u.technicalStage}</p>}<h3>{u.title}</h3><p>{u.explanation}</p></li>)}</ol>:<p>No published opportunity updates yet.</p>;
}
